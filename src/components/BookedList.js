import React from 'react';
import styled from 'styled-components';

function BookedList() {
  return <ListBox>예약 내역</ListBox>;
}

export default BookedList;

const ListBox = styled.div`
  width: 100%;
`;
